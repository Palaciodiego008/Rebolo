package models

import (
	"time"
)

type {{.Name}} struct {
	ID        int64     `json:"id"`
{{range .Fields}}	{{.Name | title}}    {{.GoType}}   `json:"{{.DBName}}"`
{{end}}	CreatedAt time.Time `json:"created_at"`
	UpdatedAt time.Time `json:"updated_at"`
}
